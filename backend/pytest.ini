[pytest]
# asyncio_mode = auto lets us write `async def test_...` without decorating each one.
asyncio_mode = auto
# Put the backend root on sys.path so tests can `import app.*` when run from backend/.
pythonpath = .
testpaths = tests
