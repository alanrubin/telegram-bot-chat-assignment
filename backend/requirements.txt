# --- Runtime ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-telegram-bot>=21.0,<22.0
pydantic-settings>=2.4,<3.0

# --- Tests (kept here so `pip install -r requirements.txt && pytest` works in one step) ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # required by Starlette's TestClient (WebSocket integration tests)
